### Modifications

The model was at some point available through BMA, but those URLs no longer work. As such, it was instead reconstructed from the rule table that is presented directly in the paper. A transcription of the table is given in `source.raw.txt`.