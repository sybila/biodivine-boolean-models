### Modifications

The model is translated by hand from attached Supplementary table S2. Note that you can also find versions of the model with newer naming of variables. Here, we use the one given in the original paper.

Finally, regulation `SCL -> FLI1` has been marked as non-essential.